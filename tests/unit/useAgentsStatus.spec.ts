/**
 * Tests unitaires pour le composable useAgentsStatus
 * Gère le fetch + polling des statuts agents
 * 
 * Note: Tests désactivés - nécessite MSW (npm install msw)
 */

import { describe, it } from 'vitest'

describe('useAgentsStatus', () => {
  it.todo('install msw to enable fetch mocking tests')
})
